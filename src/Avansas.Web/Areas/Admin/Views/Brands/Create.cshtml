@model Avansas.Application.DTOs.CreateBrandDto
@{
    ViewData["Title"] = "Yeni Marka";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h4 class="fw-bold mb-0">Yeni Marka</h4>
    <a asp-action="Index" class="btn btn-outline-secondary"><i class="fas fa-arrow-left me-2"></i>Geri</a>
</div>

<div class="card shadow-sm">
    <div class="card-body p-4">
        <form method="post">
            @Html.AntiForgeryToken()
            <div class="row g-3">
                <div class="col-md-6">
                    <label class="form-label fw-semibold">Marka Adı *</label>
                    <input id="nameInput" asp-for="Name" class="form-control" required />
                </div>
                <div class="col-md-6">
                    <label class="form-label fw-semibold">Slug</label>
                    <input id="slugInput" asp-for="Slug" class="form-control" placeholder="otomatik-oluşturulur" />
                    <div class="form-text" id="slugPreview"></div>
                </div>
                <div class="col-md-8">
                    <label class="form-label fw-semibold">Logo URL</label>
                    <input asp-for="LogoUrl" class="form-control" placeholder="https://..." />
                </div>
                <div class="col-md-4 d-flex align-items-end">
                    <div class="form-check mb-2">
                        <input asp-for="IsActive" class="form-check-input" type="checkbox" checked />
                        <label asp-for="IsActive" class="form-check-label">Aktif</label>
                    </div>
                </div>
                <div class="col-12">
                    <label class="form-label fw-semibold">Açıklama</label>
                    <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                </div>
            </div>
            <div class="mt-4">
                <button type="submit" class="btn btn-primary px-4">Kaydet</button>
                <a asp-action="Index" class="btn btn-outline-secondary ms-2">İptal</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
<script>
let manual = false;
function toSlug(v) {
    return v.toLowerCase()
        .replace(/ş/g,'s').replace(/ğ/g,'g').replace(/ü/g,'u')
        .replace(/ö/g,'o').replace(/ç/g,'c').replace(/ı/g,'i')
        .replace(/[^a-z0-9\s-]/g,'').replace(/\s+/g,'-').replace(/-+/g,'-');
}
document.getElementById('nameInput').addEventListener('input', e => {
    if (!manual) {
        const s = toSlug(e.target.value);
        document.getElementById('slugInput').value = s;
        document.getElementById('slugPreview').textContent = s ? '/marka/' + s : '';
    }
});
document.getElementById('slugInput').addEventListener('input', e => {
    manual = true;
    document.getElementById('slugPreview').textContent = e.target.value ? '/marka/' + e.target.value : '';
});
</script>
}
